{% extends 'base.html.twig' %}

{% block title %}Edit Location{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center site_title">Edit Location</h1>

    {{ include('location/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex justify-content-center">
        <a href="{{ path('location_index') }}" class="btn btn-primary">back to list</a>
    </div></br>

    {{ include('location/_delete_form.html.twig') }}
{% endblock %}
